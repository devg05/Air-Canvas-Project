1) Install all the required packages using:

	pip install -r requirements.txt

2) Create .env file and save it after replacing the contents inside <<>> of env_example.txt

3) Start the Flask Server using:

	python app.py